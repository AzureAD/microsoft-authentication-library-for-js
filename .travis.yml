language: node_js

node_js:
  - 'stable'
  - '6'
  - '4'

sudo: false

install:
  - npm install
  - npm install -g karma 
  - npm install -g karma-cli
  - npm install -g karma-chrome-launcher

cache:
  directories:
    - node_modules
