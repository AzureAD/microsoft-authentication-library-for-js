{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome against localhost",
            "url": "https://localhost:3000/?slice=testslice",
            "webRoot": "${workspaceFolder}"
        },
        {
            "type": "node-terminal",
            "name": "Run Hybrid + WAM Sample Script: start",
            "request": "launch",
            "command": "npm start -- -h",
            "cwd": "${workspaceFolder}\\samples\\msal-browser-samples\\HybridSample"
        }
    ]
}