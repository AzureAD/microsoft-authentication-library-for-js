I’ll pull the upstream findings and relevant repo instructions first, then patch the specific failing file(s).

No Forge findings were recorded, so I’m checking the PR diff and GitHub review/check context to identify the actionable failure for this branch.

The GitHub CLI query used an unsupported field, so I’m retrying with supported PR fields and reading the changed source/tests around the diff.

I’ve loaded the msal-common instructions; now I’m narrowing the failing check output before editing.

No failed check is reported through the supported PR check field, so I’m checking review text/comments directly for the actionable requested change.

The diff suggests the likely blocker is hardcoded metadata lookup for the legacy China alias after removing its endpoint entry, so I’m tracing that path and running the targeted tests.

I found the review’s two suppressed-but-actionable comments and will apply those surgical test cleanup fixes.

The review cleanup is patched; I’m running targeted validation from the affected package.

The targeted test exposed that the duplicate alias was only reachable through the new duplicate host constant, so I’m removing the paired duplicate host entry as well.

The duplicate constants are now removed; I’m rerunning the targeted checks.

The targeted checks pass; I’m doing a final diff/status check before handing off.

The formal Forge findings list was empty, but I fixed the two review comments found in the PR review body and am recording those decisions for the loop.

Fixed the PR review issues.

Updated:
- `lib/msal-common/test/authority/AuthorityMetadata.spec.ts` to remove obsolete China issuer special-casing.
- `lib/msal-common/test/test_kit/StringConstants.ts` to remove duplicate `ChinaCloudPreferred` host/alias entries.

Recorded both fixes with `decide_finding()`. Targeted authority tests and formatting check pass.