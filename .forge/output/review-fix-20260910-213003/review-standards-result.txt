```json:findings
[
  {
    "id": "browser-changefile-missing-pr-link",
    "severity": "🔴",
    "file": "change/@azure-msal-browser-3677519-dpop-nonce.json",
    "line": 3,
    "action": "fix",
    "details": {
      "problem": "The browser package changefile comment does not include the required linked PR number.",
      "reasoning": "The documented changefile convention requires every change message to end with a description followed by a linked PR number. Without it, release notes lose their required traceability to the implementation review.",
      "suggestion": "Append `[#8819](https://github.com/AzureAD/microsoft-authentication-library-for-js/pull/8819)` to the comment."
    },
    "confidence": "high"
  },
  {
    "id": "common-changefile-missing-pr-link",
    "severity": "🔴",
    "file": "change/@azure-msal-common-3677519-dpop-nonce.json",
    "line": 3,
    "action": "fix",
    "details": {
      "problem": "The common package changefile comment does not include the required linked PR number.",
      "reasoning": "The documented changefile convention requires every change message to end with a description followed by a linked PR number. Without it, release notes lose their required traceability to the implementation review.",
      "suggestion": "Append `[#8819](https://github.com/AzureAD/microsoft-authentication-library-for-js/pull/8819)` to the comment."
    },
    "confidence": "high"
  },
  {
    "id": "missing-public-dpop-nonce-documentation",
    "severity": "🔴",
    "file": "lib/msal-browser/src/cache/TokenCache.ts",
    "line": 57,
    "action": "fix",
    "details": {
      "problem": "The new public `ITokenCache.loadDpopNonce` API is not documented in the msal-browser documentation.",
      "reasoning": "Library guidance requires public API changes to be documented. The existing `lib/msal-browser/docs/shr-server-nonce.md` documents the closely related `AuthenticationHeaderParser` workflow, but it does not show how callers persist a `DPoP-Nonce` response value through the PCA-bound token cache. Consumers therefore have no supported usage guidance for the newly exposed API.",
      "suggestion": "Update `lib/msal-browser/docs/shr-server-nonce.md` or add a DPoP-specific guide that demonstrates parsing `DPoP-Nonce` with `AuthenticationHeaderParser.getDPoPNonce()` and persisting it with `pca.getTokenCache().loadDpopNonce(resourceRequestUri, nonce)`."
    },
    "confidence": "high"
  },
  {
    "id": "missing-public-token-cache-integration-test",
    "severity": "🔴",
    "file": "lib/msal-browser/src/cache/TokenCache.ts",
    "line": 57,
    "action": "fix",
    "details": {
      "problem": "The new public PCA token-cache operation lacks a test that exercises the public `pca.getTokenCache().loadDpopNonce()` path.",
      "reasoning": "The added cache tests validate `BrowserCacheManager` directly, while the public API is implemented through a new `TokenCache` wrapper and controller delegation. A regression in PCA-to-controller-to-cache wiring would not be detected by the current tests.",
      "suggestion": "Add a browser unit test that creates and initializes a `PublicClientApplication`, calls `getTokenCache().loadDpopNonce(...)`, and verifies the expected nonce is available through the bound browser cache manager."
    },
    "confidence": "high"
  }
]
```