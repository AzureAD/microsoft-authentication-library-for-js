I’m applying the eight blocking fixes together, starting from the exact branch diffs and local package rules.

The fixes converge on two ownership rules: only explicit caller ownership suppresses key cleanup, and generated broker keys must remain request-addressable without surviving the memory token cache. I’m tracing the existing request/cache helpers and tests before editing.

I’m choosing the safer contract-compatible path: generated fallback keys and tokens stay page-operation-local and are removed after use; only caller-supplied key partitions are cacheable/reusable. This fixes both the write-only cache and persistent orphan problems without adding a new durable mapping.

The ownership fix is in place; I’m checking the edited control flow now and will repair any patch-boundary issues before updating focused regression tests.

The regression coverage now asserts explicit L1 selection, no whole-account DPoP eviction, generated-key cleanup without caching, and legacy key removal. I’m running the two focused suites plus formatting/type validation.

The focused suites pass. Only Prettier reported changes, so I’m applying repository formatting and then running package lint/type builds before recording all finding decisions.

All edited packages now type-check and lint without errors. I’m doing the final surgical diff review and recording the eight fixes against their finding IDs.

Fixed all eight blocking findings for PR #8789.

Key changes: explicit L1 broker confirmation is required; generated DPoP keys/tokens are no longer retained in the memory-cache path; DPoP partitions no longer clear the whole account context; legacy keys are cleaned unless explicitly caller-owned; documentation, tests, TSDoc, and the changefile PR link were updated. All eight findings were recorded as fixed. Targeted tests passed: 108 browser tests and 87 common-cache tests; type builds and lint completed without errors.