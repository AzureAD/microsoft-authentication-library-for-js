I’m loading the mandatory security and repository guidance before reviewing the authoritative change set.

The review scope is confirmed. I’m now reading every diff alongside the applicable package, browser-compatibility, documentation, and changefile rules.

Two large diffs were truncated by the tool, so I’m expanding them and sizing every changed file to ensure the review covers each file in full.

The main implementation path is now visible. I’m tracing cache reuse, broker version negotiation, key ownership, and proof generation against the surrounding code before recording only verified regressions.

I’ve isolated three concrete lifecycle and validation failures. I’m verifying the backing key-store lifetime and non-broker DPoP creation path so the findings state the exact impact without speculation.

```json:findings
[
  {
    "id": "dpop-key-ownership-default-breaks-existing-cleanup",
    "severity": "🟡",
    "file": "lib/msal-common/src/cache/CacheManager.ts",
    "line": 1147,
    "action": "fix",
    "details": {
      "problem": "The new ownership guard disables key cleanup for all existing non-broker DPoP cache entries because those entries never populate `tokenBindingKeyOwnedByMsal`.",
      "reasoning": "ResponseHandler creates standard DPoP access-token entities with a keyId but no ownership marker. The changed `=== true` condition therefore evaluates false, so token eviction deletes the access token while leaving the corresponding persistent token-binding key indefinitely.",
      "suggestion": "Preserve cleanup for legacy/MSAL-generated entries and use an explicit caller-owned value to opt out, or set the ownership field in every DPoP token creation path and add upgrade/legacy-cache tests."
    },
    "confidence": "high"
  },
  {
    "id": "generated-broker-dpop-cache-is-write-only",
    "severity": "🟡",
    "file": "lib/msal-browser/src/interaction_client/PlatformAuthInteractionClient.ts",
    "line": 234,
    "action": "fix",
    "details": {
      "problem": "MSAL-generated broker DPoP tokens are cached under a generated key that subsequent requests can never use for cache lookup.",
      "reasoning": "The cache decision occurs before `prepareDpopBrokerRequest`, so requests without `popKid` always have no `keyId` and skip `acquireTokensFromCache`. The generated key is retained after caching but is not stored in any request-to-partition mapping, causing every subsequent call to provision a new key and contact the broker.",
      "suggestion": "Establish or recover the generated DPoP key partition before cache lookup, or do not cache/retain generated L1 entries until they can be addressed by later requests; add a two-call cache-hit test without caller-supplied `popKid`."
    },
    "confidence": "high"
  },
  {
    "id": "broker-dpop-key-outlives-memory-token-cache",
    "severity": "🟡",
    "file": "lib/msal-browser/src/interaction_client/PlatformAuthInteractionClient.ts",
    "line": 1647,
    "action": "fix",
    "details": {
      "problem": "A successful L1 broker flow retains its generated key in persistent storage even though the associated access token exists only in a page-local memory cache.",
      "reasoning": "`nativeInternalStorage` uses `MemoryStorage`, but `TokenBindingKeyManager` is backed by IndexedDB. `retainDpopRequestKey` removes the only cleanup state once the cache owns the key; a reload silently drops the access token without invoking `removeAccessToken`, leaving the private key orphaned across page lifetimes.",
      "suggestion": "Align key and token lifetimes or persist cleanup ownership so startup/page-lifecycle reconciliation can remove keys whose native memory-cache token no longer exists."
    },
    "confidence": "high"
  },
  {
    "id": "ambiguous-dpop-response-treated-as-l1",
    "severity": "🟡",
    "file": "lib/msal-browser/src/interaction_client/PlatformAuthInteractionClient.ts",
    "line": 1506,
    "action": "fix",
    "details": {
      "problem": "An incomplete DPoP broker response is accepted as an L1 fallback even when the broker never explicitly chose local binding.",
      "reasoning": "For `{ token_type: \"DPoP\" }`, all current rejection predicates are false. The client consequently assumes the token is bound to `request.keyId`, signs a proof with that key, and caches the token, which can produce unusable credentials from malformed or incompatible broker responses.",
      "suggestion": "Require `attested_chosen === false` for L1 and validate `token_binding_key_id` against the requested local key according to the broker contract; add a missing-outcome-field rejection test."
    },
    "confidence": "high"
  }
]
```