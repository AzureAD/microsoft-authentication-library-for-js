{
	"devDependencies": {
		"lerna": "^2.9.0"
	},
	"scripts": {
		"test": "cd lib/msal-core && npm install && npm run build && cd ../msalAngular && npm install && npm run build && cd ../msalAngularjs && npm install && npm run test"
	}
}

