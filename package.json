{
  "name": "microsoft-authentication-libraries-for-js",
  "private": true,
  "devDependencies": {
    "lerna": "^3.16.4"
  },
  "scripts": {
    "postinstall": "npm run bootstrap:libraries",
    "clean": "lerna run clean --no-private && lerna clean",
    "bootstrap:libraries": "lerna bootstrap --no-private",
    "bootstrap:samples": "lerna bootstrap && lerna run build:modules --no-private"
  },
  "dependencies": {}
}
