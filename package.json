{
	"devDependencies": {
		"lerna": "^2.9.0"
	},
  "scripts": {
    "test": "cd lib/msal-core && npm install && npm run build && cd ../msalAngular && npm install && npm run ngcompile && npm run test && cd ../msalAngularjs && npm install && npm rebuild && npm run test",
     "endToEnd": "cd lib/msalAngular/tests/endToEndTests && npm install &&  npm run endToEnd"
  }
}

