import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'windows-native-sample',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
