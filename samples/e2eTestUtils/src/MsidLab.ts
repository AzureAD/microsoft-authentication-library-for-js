export type MsidLab = {
    labName?: string,
    region?: string,
    id?: number,
    tenantId?: string,
    federationProvider?: string,
    azureEnvironment?: string,
    credentialKeyVaultKeyName?: string,
    authority?: string,
    adfsEndpoint?: string
};
