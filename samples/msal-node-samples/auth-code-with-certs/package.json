{
  "name": "msal-node-auth-code-certs",
  "version": "1.0.0",
  "description": "sample for msal-node confidential client application using certificates",
  "main": "index.js",
  "private": true,
  "scripts": {
    "start": "node ./index.js",
    "build:package": "cd ../../../lib/msal-common && npm run build && cd ../msal-node && npm run build",
    "start:build": "npm run build:package && npm start"
  },
  "author": "Microsoft",
  "license": "MIT",
  "dependencies": {
    "@azure/msal-node": "^3.0.0",
    "express": "^4.20.0",
    "local": "^0.3.3"
  }
}