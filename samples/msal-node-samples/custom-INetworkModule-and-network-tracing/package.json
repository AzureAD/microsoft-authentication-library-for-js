{
  "name": "custom-inetworkmodule-and-network-tracing",
  "version": "1.0.0",
  "description": "Custom INetworkModule Sample & Network Tracing via Fiddler Everywhere",
  "scripts": {
    "build": "npx tsc",
    "start": "npm run build && node dist/app.js",
    "build:package": "cd ../../../lib/msal-common && npm run build && cd ../msal-node && npm run build",
    "install:common": "npm install ../../../lib/msal-common",
    "install:node": "npm install ../../../lib/msal-node"
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@azure/msal-common": "file:../../../lib/msal-common",
    "@azure/msal-node": "file:../../../lib/msal-node",
    "url": "^0.11.0"
  },
  "devDependencies": {
    "@types/node": "^18.11.3",
    "typescript": "^4.8.4"
  }
}
