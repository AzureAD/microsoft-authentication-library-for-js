comment:
  layout: "flags, files"
  behavior: default
  require_changes: false  
  require_base: no
  require_head: no
  show_carryforward_flags: true

flag_management:
  default_rules:
    carryforward: true